case_id: P4-T4-IG-PASS
target_mode: web_url
env_id: ENV-SQLITE
run_date: 20260318
steps -> interaction result -> conclusion

1. 打开 `http://127.0.0.1:3000/?runtime_mode=sqlite_only&mock_session=p4t4-ig-pass` 并开始录屏。
2. 键盘下移到较旧系列 `Project-A`，直接输入并提交 `z reorder note`。
3. 提交完成后，`Project-A` 置顶，顶部摘录刷新为 `z reorder note`。
4. 继续用 `ArrowRight` 打开时间线，首条只读记录即为 `z reorder note`，随后 `Escape` 返回列表。
5. 录屏归档为 `qa-gates-codex/P4-T4-IG-PASS_20260318_ENV-SQLITE_codex.mp4`。

actual_result:
- topExcerpt: `z reorder note`
- firstTimeline: `z reorder note`

conclusion: PASS
