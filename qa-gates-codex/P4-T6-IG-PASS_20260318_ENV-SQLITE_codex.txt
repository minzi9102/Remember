case_id: P4-T6-IG-PASS
target_mode: web_url
env_id: ENV-SQLITE
run_date: 20260318
steps -> visible result -> conclusion

1. 打开 `http://127.0.0.1:3000/?runtime_mode=sqlite_only&mock_session=p4t6-ig-pass` 并开始录屏。
2. 在 Active 视图选中 `Project-A` 并按下 `a`，确认该系列从主列表隐藏。
3. 切换到 `Archived`，打开 `Project-A` 的时间线并确认只读提示存在。
4. 点击返回，确认列表仍停留在 `Archived` 且选择保持在 `Project-A`。
5. 录屏归档为 `qa-gates-codex/P4-T6-IG-PASS_20260318_ENV-SQLITE_codex.mp4`。

actual_result:
- archivedSelected: `Selected archived series: Project-A`
- timelineHint: `Archived timeline is read-only. \`←\` or \`Esc\` returns.`

conclusion: PASS
