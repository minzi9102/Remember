case_id: P4-T5-VG-PASS
target_mode: web_url
env_id: ENV-SQLITE
run_date: 20260318
steps -> visible result -> conclusion

1. 打开 `http://127.0.0.1:3000/?runtime_mode=sqlite_only&mock_session=p4t5-vg-pass`。
2. 等待一级列表完成加载，并确认 `Project-A` 行出现 `Silent` 标签。
3. 读取主列表顺序，确认顶部仍为 `Inbox`，沉寂系列位于其下方。
4. 截图归档为 `qa-gates-codex/P4-T5-VG-PASS_20260318_ENV-SQLITE_codex.png`。

actual_result:
- topRow: `series-row-series-inbox`
- silentRow: `series-row-series-project-a`
- silentBadge: `Silent`

conclusion: PASS
