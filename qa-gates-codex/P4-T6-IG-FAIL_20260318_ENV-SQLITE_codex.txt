case_id: P4-T6-IG-FAIL
target_mode: web_url
env_id: ENV-SQLITE
run_date: 20260318
steps -> visible result -> conclusion

1. 打开 `http://127.0.0.1:3000/?runtime_mode=sqlite_only&mock_session=p4t6-ig-fail` 并开始录屏。
2. 将 `Project-A` 归档并切换到 `Archived` 视图。
3. 依次按下 `x`、`Shift+N`、`a`，确认三次都被 `ARCHIVE_READ_ONLY` 拦截。
4. 切换回 `Active`，执行一次合法的新建与提交，确认系统可恢复并把新系列置顶。
5. 录屏归档为 `qa-gates-codex/P4-T6-IG-FAIL_20260318_ENV-SQLITE_codex.mp4`。

actual_result:
- blockedFromType: `ARCHIVE_READ_ONLYarchived series are read-only; switch to Active to make changes`
- blockedFromCreate: `ARCHIVE_READ_ONLYarchived series are read-only; switch to Active to make changes`
- blockedFromArchive: `ARCHIVE_READ_ONLYarchived series are read-only; switch to Active to make changes`
- topRowAfterRecovery: `P4T6 Recoveryrecovered commitView timeline`

conclusion: PASS
