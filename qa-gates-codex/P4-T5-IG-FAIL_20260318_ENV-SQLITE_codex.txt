case_id: P4-T5-IG-FAIL
target_mode: web_url
env_id: ENV-SQLITE
run_date: 20260318
steps -> visible result -> conclusion

1. 打开 `http://127.0.0.1:3000/?runtime_mode=sqlite_only&mock_session=p4t5-ig-fail&rpc_error=dual_write_failed&rpc_error_path=series.scan_silent` 并开始录屏。
2. 等待初始错误反馈出现，确认扫描失败被显式展示。
3. 在失败状态下继续对 `Project-A` 提交合法内容 `recovery note`。
4. 提交后等待列表刷新，确认 `Project-A` 仍能升至顶部且沉寂标签消失，同时扫描失败反馈仍可见。
5. 录屏归档为 `qa-gates-codex/P4-T5-IG-FAIL_20260318_ENV-SQLITE_codex.mp4`。

actual_result:
- initialFeedbackCode: `DUAL_WRITE_FAILED`
- legalRecoveryCompleted: `true`
- topRowAfterRecovery: `series-row-series-project-a`
- silentBadgeAfterRecovery: `removed`
- feedbackAfterRecovery: `DUAL_WRITE_FAILED`

conclusion: PASS
